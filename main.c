#include <stdio.h>
#include <stdlib.h>
#include "Hash_enca.h"

int main(){
	Clave *clave;
	Nodo *tabla[TAMANO];//hash_enca
	int k;//hash_enca
//	clave=inicializa_clave(6);
//	tabla[TAMANO]=inicializa_tabla();
//	k=hash(clave);//hash_enca
//	tabla[k]=completar_tabla(tabla,clave);//hash_enca
//	printf("%i\n",k);
	//llenar_tabla(tabla[TAMANO],clave,k);
}
